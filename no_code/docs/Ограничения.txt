Ограничения

В субпроцесс передаются только строки
    из строки вытаскивается сслыка на класс
    нет ссылок на объект
    субпроцессы это микросервисы, которые объединяют результат в предложение
Завести ноды экземпляр и класс
    реализовать операции сравнения любой ноды с любой
    по названию
    по типу (класс, объект)
В качестве параметра функции можно передавать кусок графа
    есть функции для обработки одного предложения
    в них можно ограничиться названием нод
    если есть односзначное ссоответсвие (например только классы)
    есть функции для обработки графа
сохранение определния в виде нескольких предложений

классы
    defenition (поле)
    +graph
    +class
    +object
    +link
    programm_python (поле)
    first_born
    second_born
    character (поле)
    +node
    +alphabet


делаем свою рисовалку

создание алфавита тоже должно быть второродным

функция для вычитания отличий объекта и записи в класс
    операции над множествами
    у признака может быть степень выраженности
    операция чтения, изменения, ... и уточнения определения

Предлагаю запилить все три функции
- сравнение по имени
- сравнение по типу класса
- сравнение по вложенности иерархии (позже сделаю и подумаю еще)

